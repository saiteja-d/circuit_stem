import 'package:flutter/material.dart';

class DarkModeColors {
  static const Color wirePowered = Color(0xFFFFD700);     // Gold
  static const Color wireUnpowered = Color(0xFF808080);   // Gray
  static const Color componentActive = Color(0xFF00FF00);  // Green
  static const Color componentInactive = Color(0xFFFF0000); // Red
  static const Color onPrimary = Color(0xFFFFFFFF);        // White
}

class LightModeColors {
  static const Color wirePowered = Color(0xFFFFD700);      // Gold
  static const Color wireUnpowered = Color(0xFF505050);    // Dark Gray
  static const Color componentActive = Color(0xFF00CC00);   // Dark Green
  static const Color componentInactive = Color(0xFFCC0000); // Dark Red
  static const Color onPrimary = Color(0xFF000000);        // Black
}
